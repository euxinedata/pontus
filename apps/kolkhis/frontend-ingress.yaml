apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kolkhis-frontend
  namespace: kolkhis
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - app.euxine.eu
      secretName: kolkhis-frontend-tls
  rules:
    - host: app.euxine.eu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kolkhis-frontend
                port:
                  number: 80
